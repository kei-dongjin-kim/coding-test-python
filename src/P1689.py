class Solution:
    def minPartitions(self, n: str) -> int:
        # max_val = 0
        # for c in n:
        #     max_val = max(max_val, int(c))
        # return max_val
        return int(max(n))